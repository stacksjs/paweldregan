{
  "name": "paweldregan",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "description": "Paweł Dregan — Ultra Runner. Marketing site.",
  "scripts": {
    "dev": "bun --bun scripts/dev.ts",
    "build": "bun --bun scripts/build.ts",
    "deploy": "bun --bun scripts/deploy.ts",
    "preview": "bun --bun scripts/preview.ts",
    "typecheck": "bun --bun tsc --noEmit",
    "verify": "bun --bun scripts/verify.ts"
  },
  "dependencies": {
    "@stacksjs/stx": "^0.2.35",
    "@stacksjs/ts-cloud": "^0.2.10"
  },
  "devDependencies": {
    "@types/bun": "^1.1.13",
    "typescript": "^5.9.3"
  }
}
